import React,{useState}from'react'; import{Alert,ScrollView}from'react-native'; import{SafeAreaView}from'react-native-safe-area-context'; import Button from'@/components/Button'; import Input from'@/components/Input'; import{ScreenHeader}from'@/components/business-ui'; import{useAuthStore}from'@/store/authStore';
export default function BusinessConfig(){const business=useAuthStore(s=>s.business),update=useAuthStore(s=>s.updateBusiness);const[name,setName]=useState(business?.name??''),[category,setCategory]=useState(business?.category??''),[country,setCountry]=useState(business?.country??''),[currency,setCurrency]=useState(business?.currency??'NGN'),[branch,setBranch]=useState(business?.branchName??'');const save=()=>{if(!business||![name,category,country,currency,branch].every(v=>v.trim()))return Alert.alert('Complete all fields');update({...business,name:name.trim(),category:category.trim(),country:country.trim(),currency:currency.trim(),branchName:branch.trim()});Alert.alert('Saved','Business profile updated.');};return <SafeAreaView className="flex-1 bg-[#F5F7FB]" edges={['top']}><ScrollView contentContainerClassName="gap-4 px-5 pb-28 pt-5"><ScreenHeader title="Business profile"/><Input label="Business name" value={name} onChangeText={setName}/><Input label="Category" value={category} onChangeText={setCategory}/><Input label="Country" value={country} onChangeText={setCountry}/><Input label="Currency code" value={currency} onChangeText={setCurrency}/><Input label="Primary branch" value={branch} onChangeText={setBranch}/><Button title="Save changes" onPress={save}/></ScrollView></SafeAreaView>}
