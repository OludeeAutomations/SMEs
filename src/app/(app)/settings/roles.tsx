import React,{useState}from'react'; import{Alert,ScrollView}from'react-native'; import{SafeAreaView}from'react-native-safe-area-context'; import Button from'@/components/Button'; import Input from'@/components/Input'; import{ChoiceChips,DataRow,Divider,EmptyState,ScreenHeader}from'@/components/business-ui'; import{SurfaceCard}from'@/components/dashboard-ui'; import{useBusinessStore,useWorkspace}from'@/store/businessStore';
export default function RolesScreen(){const w=useWorkspace(),add=useBusinessStore(s=>s.addTeamMember);const[name,setName]=useState(''),[email,setEmail]=useState(''),[role,setRole]=useState('Cashier');const save=()=>{if(!name.trim()||!email.includes('@'))return Alert.alert('Enter a valid name and email');add(name.trim(),email.trim(),role);setName('');setEmail('')};const members=w.teamMembers??[];return <SafeAreaView className="flex-1 bg-[#F5F7FB]" edges={['top']}><ScrollView contentContainerClassName="gap-4 px-5 pb-28 pt-5"><ScreenHeader title="Team and roles" subtitle="Add people who work in this business."/><SurfaceCard className="gap-3"><Input label="Name" value={name} onChangeText={setName} placeholder="Team member"/><Input label="Email" value={email} onChangeText={setEmail} keyboardType="email-address" placeholder="name@business.com"/><ChoiceChips options={['Manager','Cashier','Storekeeper']} value={role} onChange={setRole}/><Button title="Add team member" onPress={save}/></SurfaceCard>{members.length?<SurfaceCard className="py-0">{members.map((m,i)=><React.Fragment key={m.id}><DataRow title={m.name} subtitle={m.email} value={m.role}/>{i<members.length-1?<Divider/>:null}</React.Fragment>)}</SurfaceCard>:<EmptyState title="No team members" message="Only the workspace owner currently has access."/>}</ScrollView></SafeAreaView>}
