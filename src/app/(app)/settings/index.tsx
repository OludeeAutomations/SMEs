import React from'react';import{Pressable,ScrollView,Text,View}from'react-native';import{SafeAreaView}from'react-native-safe-area-context';import{useRouter}from'expo-router';import{ChevronRight}from'lucide-react-native';import{colors,SurfaceCard}from'@/components/dashboard-ui';
const sections=[['BUSINESS PROFILE',[['Company information','/(app)/settings/business-config'],['Branch management','/(app)/settings/business-config'],['Team management','/(app)/settings/roles']]],['SECURITY',[['Password and OTP','/(auth)/reset'],['Device sessions','/(app)/settings/roles'],['Permissions','/(app)/settings/roles']]],['PREFERENCES',[['Notifications','/(app)/settings/notifications'],['Subscription','/(app)/settings/payments'],['Integrations','/(app)/settings/payments']]]] as const;
export default function EaseSettingsScreen(){const router=useRouter();return <SafeAreaView className="flex-1 bg-[#F5F7FB]" edges={['top']}><ScrollView contentContainerClassName="gap-4 px-5 pb-8 pt-5"><Text className="text-2xl font-bold text-[#0F172A]">Settings</Text>{sections.map(([label,items])=><SurfaceCard key={label} className="py-2"><Text className="text-[10px] font-bold text-[#475569]">{label}</Text>{items.map(([title,route])=><Pressable key={title} onPress={()=>router.push(route)} className="flex-row items-center justify-between py-3"><Text className="text-[13px] font-semibold text-[#0F172A]">{title}</Text><ChevronRight size={17} color={colors.muted}/></Pressable>)}</SurfaceCard>)}</ScrollView></SafeAreaView>}
