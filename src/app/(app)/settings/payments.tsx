import React,{useState}from'react'; import{Alert,ScrollView,Text,View}from'react-native'; import{SafeAreaView}from'react-native-safe-area-context'; import Button from'@/components/Button'; import Input from'@/components/Input'; import{ChoiceChips,ScreenHeader}from'@/components/business-ui'; import{useBusinessStore,useWorkspace}from'@/store/businessStore';
export default function PaymentsScreen(){const w=useWorkspace(),setPreference=useBusinessStore(s=>s.setPreference),preferences=w.preferences??{};const[provider,setProvider]=useState(String(preferences.paymentProvider??'Paystack')),[publicKey,setPublicKey]=useState(String(preferences.paymentPublicKey??''));const save=()=>{if(!publicKey.startsWith('pk_'))return Alert.alert('Invalid public key','Enter the provider public key beginning with pk_.');setPreference('paymentProvider',provider);setPreference('paymentPublicKey',publicKey.trim());Alert.alert('Payment setup saved','Secret keys must remain on your secure backend and are never stored in this app.');};return <SafeAreaView className="flex-1 bg-[#F5F7FB]" edges={['top']}><ScrollView contentContainerClassName="gap-4 px-5 pb-28 pt-5"><ScreenHeader title="Payment setup" subtitle="Configure the public client identifier for checkout."/><ChoiceChips options={['Paystack','Flutterwave']} value={provider} onChange={setProvider}/><Input label="Public key" placeholder="pk_test_..." value={publicKey} onChangeText={setPublicKey}/><View className="rounded-[5px] border border-[#A7F3D0] bg-[#ECFDF5] p-4"><Text className="text-xs leading-[18px] text-[#047857]">For security, secret keys belong in a server environment—not in a phone app.</Text></View><Button title="Save payment setup" onPress={save}/></ScrollView></SafeAreaView>}
