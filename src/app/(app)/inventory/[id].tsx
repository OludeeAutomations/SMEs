import React,{useState}from'react'; import{Alert,ScrollView}from'react-native'; import{SafeAreaView}from'react-native-safe-area-context'; import{useLocalSearchParams}from'expo-router'; import Button from'@/components/Button'; import Input from'@/components/Input'; import{EmptyState,ScreenHeader}from'@/components/business-ui'; import{MetricCard,SurfaceCard,colors}from'@/components/dashboard-ui'; import{useAuthStore}from'@/store/authStore'; import{useBusinessStore,useWorkspace}from'@/store/businessStore'; import{formatMoney,parseAmount}from'@/utils/format';
export default function ProductDetail(){const{id}=useLocalSearchParams<{id:string}>(),w=useWorkspace(),p=w.products.find(x=>x.id===id),adjustStock=useBusinessStore(s=>s.adjustStock),currency=useAuthStore(s=>s.business?.currency??'NGN');const[quantity,setQuantity]=useState('');if(!p)return <SafeAreaView className="flex-1 bg-[#F5F7FB]" edges={['top']}><EmptyState title="Product not found" message="This product may no longer exist."/></SafeAreaView>;const adjust=(sign:number)=>{const value=parseAmount(quantity);if(!value)return Alert.alert('Enter a quantity');adjustStock(p.id,sign*value);setQuantity('')};return <SafeAreaView className="flex-1 bg-[#F5F7FB]" edges={['top']}><ScrollView contentContainerClassName="gap-4 px-5 pb-28 pt-5"><ScreenHeader title={p.name} subtitle={p.category}/><MetricCard label="In stock" value={`${p.stockQuantity} units`} color={p.stockQuantity<=p.lowStockThreshold?colors.amber:colors.green}/><MetricCard label="Selling price" value={formatMoney(p.sellingPrice,currency)} color={colors.blue}/><SurfaceCard className="gap-3"><Input label="Adjustment quantity" placeholder="0" value={quantity} onChangeText={setQuantity} keyboardType="numeric"/><Button title="Add stock" onPress={()=>adjust(1)}/><Button title="Remove stock" variant="secondary" onPress={()=>adjust(-1)}/></SurfaceCard></ScrollView></SafeAreaView>}
