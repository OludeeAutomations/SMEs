import React from'react'; import{ScrollView}from'react-native'; import{SafeAreaView}from'react-native-safe-area-context'; import{useRouter}from'expo-router'; import{DataRow,Divider,EmptyState,ScreenHeader}from'@/components/business-ui'; import{SurfaceCard}from'@/components/dashboard-ui'; import{useWorkspace}from'@/store/businessStore'; export default function Screen(){const router=useRouter(),items=useWorkspace().products.filter(p=>p.stockQuantity<=p.lowStockThreshold);return <SafeAreaView className="flex-1 bg-[#F5F7FB]" edges={['top']}><ScrollView contentContainerClassName="gap-4 px-5 pb-28 pt-5"><ScreenHeader title="Low stock" subtitle="Products at or below their alert threshold."/>{items.length?<SurfaceCard className="py-0">{items.map((p,i)=><React.Fragment key={p.id}><DataRow title={p.name} subtitle={`${p.stockQuantity} left • alert at ${p.lowStockThreshold}`} onPress={()=>router.push(`/(app)/inventory/${p.id}` as never)}/>{i<items.length-1?<Divider/>:null}</React.Fragment>)}</SurfaceCard>:<EmptyState title="Stock levels look good" message="No products are currently below their threshold."/>}</ScrollView></SafeAreaView>}
