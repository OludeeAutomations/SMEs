import React,{useState}from'react'; import{ScrollView}from'react-native'; import{SafeAreaView}from'react-native-safe-area-context'; import Button from'@/components/Button'; import Input from'@/components/Input'; import{DataRow,Divider,EmptyState,ScreenHeader}from'@/components/business-ui'; import{SurfaceCard}from'@/components/dashboard-ui'; import{useBusinessStore,useWorkspace}from'@/store/businessStore'; export default function Screen(){const w=useWorkspace(),add=useBusinessStore(s=>s.addInventoryCategory),[value,setValue]=useState('');const categories=Array.from(new Set([...w.inventoryCategories,...w.products.map(p=>p.category)]));return <SafeAreaView className="flex-1 bg-[#F5F7FB]" edges={['top']}><ScrollView contentContainerClassName="gap-4 px-5 pb-28 pt-5"><ScreenHeader title="Inventory categories"/><SurfaceCard className="gap-3"><Input label="New category" value={value} onChangeText={setValue} placeholder="Category name"/><Button title="Add category" onPress={()=>{if(value.trim()){add(value.trim());setValue('')}}}/></SurfaceCard>{categories.length?<SurfaceCard className="py-0">{categories.map((c,i)=><React.Fragment key={c}><DataRow title={c} subtitle={`${w.products.filter(p=>p.category===c).length} products`}/>{i<categories.length-1?<Divider/>:null}</React.Fragment>)}</SurfaceCard>:<EmptyState title="No categories" message="Add your first inventory category."/>}</ScrollView></SafeAreaView>}
