import React from'react'; import{Alert,ScrollView,Share}from'react-native'; import{SafeAreaView}from'react-native-safe-area-context'; import{useLocalSearchParams}from'expo-router'; import Button from'@/components/Button'; import{DataRow,Divider,EmptyState,ScreenHeader}from'@/components/business-ui'; import{MetricCard,SurfaceCard,colors}from'@/components/dashboard-ui'; import{useAuthStore}from'@/store/authStore'; import{useBusinessStore,useWorkspace}from'@/store/businessStore'; import{formatDate,formatMoney}from'@/utils/format';
export default function InvoiceDetail(){const{id}=useLocalSearchParams<{id:string}>(),w=useWorkspace(),invoice=w.invoices.find(x=>x.id===id),update=useBusinessStore(s=>s.updateInvoiceStatus),currency=useAuthStore(s=>s.business?.currency??'NGN');if(!invoice)return <SafeAreaView className="flex-1 bg-[#F5F7FB]" edges={['top']}><EmptyState title="Invoice not found" message="This invoice may no longer exist."/></SafeAreaView>;const share=()=>Share.share({message:`Invoice for ${invoice.customerName}\n${invoice.items.map(i=>`${i.productName} x${i.quantity}: ${formatMoney(i.price*i.quantity,currency)}`).join('\n')}\nTotal: ${formatMoney(invoice.total,currency)}\nDue: ${formatDate(invoice.dueDate)}\nStatus: ${invoice.status}`});const paid=()=>Alert.alert('Record payment',`Mark ${formatMoney(invoice.total,currency)} as paid?`,[{text:'Cancel',style:'cancel'},{text:'Mark paid',onPress:()=>update(invoice.id,'PAID')}]);return <SafeAreaView className="flex-1 bg-[#F5F7FB]" edges={['top']}><ScrollView contentContainerClassName="gap-4 px-5 pb-28 pt-5"><ScreenHeader title={`Invoice ${invoice.id.slice(-6).toUpperCase()}`} subtitle={`${invoice.customerName} • due ${formatDate(invoice.dueDate)}`}/><MetricCard label="Total" value={formatMoney(invoice.total,currency)} color={colors.blue}/><MetricCard label="Status" value={invoice.status} color={invoice.status==='PAID'?colors.green:colors.amber}/><SurfaceCard className="py-0">{invoice.items.map((item,i)=><React.Fragment key={`${item.productId}-${i}`}><DataRow title={item.productName} subtitle={`${item.quantity} × ${formatMoney(item.price,currency)}`} value={formatMoney(item.price*item.quantity,currency)}/>{i<invoice.items.length-1?<Divider/>:null}</React.Fragment>)}</SurfaceCard>{invoice.status!=='PAID'?<Button title="Record payment" onPress={paid}/>:null}<Button title="Share invoice" variant="secondary" onPress={share}/></ScrollView></SafeAreaView>}
