import React from'react'; import{ScrollView}from'react-native'; import{SafeAreaView}from'react-native-safe-area-context'; import{useLocalSearchParams}from'expo-router'; import{EmptyState,ScreenHeader}from'@/components/business-ui'; import{MetricCard,SurfaceCard,colors}from'@/components/dashboard-ui'; import{useAuthStore}from'@/store/authStore'; import{useWorkspace}from'@/store/businessStore'; import{formatMoney}from'@/utils/format';
export default function SupplierDetail(){const{id}=useLocalSearchParams<{id:string}>(),w=useWorkspace(),s=w.suppliers.find(x=>x.id===id),currency=useAuthStore(x=>x.business?.currency??'NGN');if(!s)return <SafeAreaView className="flex-1 bg-[#F5F7FB]" edges={['top']}><EmptyState title="Supplier not found" message="This supplier may no longer exist."/></SafeAreaView>;return <SafeAreaView className="flex-1 bg-[#F5F7FB]" edges={['top']}><ScrollView contentContainerClassName="gap-4 px-5 pb-28 pt-5"><ScreenHeader title={s.name} subtitle={s.phoneNumber}/><MetricCard label="Outstanding balance" value={formatMoney(s.outstandingBalance,currency)} color={colors.amber}/><SurfaceCard><ScreenHeader title="Contact" subtitle={`${s.emailAddress||'No email'}${s.address?` • ${s.address}`:''}`}/></SurfaceCard></ScrollView></SafeAreaView>}
