import React,{useState}from'react'; import{Pressable,ScrollView}from'react-native'; import{SafeAreaView}from'react-native-safe-area-context'; import Button from'@/components/Button'; import Input from'@/components/Input'; import{DataRow,Divider,EmptyState,ScreenHeader}from'@/components/business-ui'; import{SurfaceCard}from'@/components/dashboard-ui'; import{useBusinessStore,useWorkspace}from'@/store/businessStore';
export default function ProjectsScreen(){const w=useWorkspace(),add=useBusinessStore(s=>s.addProject),toggle=useBusinessStore(s=>s.toggleProject);const[title,setTitle]=useState('');const save=()=>{if(!title.trim())return;add(title.trim());setTitle('')};return <SafeAreaView className="flex-1 bg-[#F5F7FB]" edges={['top']}><ScrollView contentContainerClassName="gap-4 px-5 pb-28 pt-5"><ScreenHeader title="Projects and tasks"/><SurfaceCard className="gap-3"><Input label="New task" placeholder="What needs to be done?" value={title} onChangeText={setTitle}/><Button title="Add task" onPress={save}/></SurfaceCard>{w.projects.length?<SurfaceCard className="py-0">{w.projects.map((p,i)=><React.Fragment key={p.id}><Pressable onPress={()=>toggle(p.id)}><DataRow title={p.completed?`✓ ${p.title}`:p.title} subtitle={p.completed?'Completed':'Tap to complete'}/></Pressable>{i<w.projects.length-1?<Divider/>:null}</React.Fragment>)}</SurfaceCard>:<EmptyState title="No tasks" message="Add a task to start planning work."/>}</ScrollView></SafeAreaView>}
