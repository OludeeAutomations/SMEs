import React from'react'; import{ScrollView}from'react-native'; import{SafeAreaView}from'react-native-safe-area-context'; import{useRouter}from'expo-router'; import{DataRow,Divider,EmptyState,ScreenHeader}from'@/components/business-ui'; import{SurfaceCard}from'@/components/dashboard-ui'; import{useAuthStore}from'@/store/authStore'; import{useWorkspace}from'@/store/businessStore'; import{formatMoney}from'@/utils/format'; export default function Screen(){const router=useRouter(),w=useWorkspace(),currency=useAuthStore(s=>s.business?.currency??'NGN'),items=w.customers.filter(c=>c.amountOwed>0);return <SafeAreaView className="flex-1 bg-[#F5F7FB]" edges={['top']}><ScrollView contentContainerClassName="gap-4 px-5 pb-28 pt-5"><ScreenHeader title="Customer balances"/>{items.length?<SurfaceCard className="py-0">{items.map((c,i)=><React.Fragment key={c.id}><DataRow title={c.fullName} subtitle={c.phoneNumber} value={formatMoney(c.amountOwed,currency)} onPress={()=>router.push(`/(app)/customers/${c.id}` as never)}/>{i<items.length-1?<Divider/>:null}</React.Fragment>)}</SurfaceCard>:<EmptyState title="No outstanding balances" message="All customer balances are settled."/>}</ScrollView></SafeAreaView>}
