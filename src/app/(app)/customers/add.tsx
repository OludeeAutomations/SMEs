import React,{useState}from'react'; import{Alert,ScrollView}from'react-native'; import{SafeAreaView}from'react-native-safe-area-context'; import{useRouter}from'expo-router';
import Button from '@/components/Button'; import Input from '@/components/Input'; import{ScreenHeader}from'@/components/business-ui'; import{useBusinessStore}from'@/store/businessStore'; import{parseAmount}from'@/utils/format';
export default function AddCustomerScreen(){const router=useRouter(),addCustomer=useBusinessStore(s=>s.addCustomer); const[name,setName]=useState(''),[phone,setPhone]=useState(''),[email,setEmail]=useState(''),[address,setAddress]=useState(''),[balance,setBalance]=useState(''); const save=()=>{if(!name.trim()||!phone.trim())return Alert.alert('Missing details','Customer name and phone number are required.');addCustomer({fullName:name.trim(),phoneNumber:phone.trim(),emailAddress:email.trim()||undefined,address:address.trim()||undefined,amountOwed:parseAmount(balance),notes:undefined});Alert.alert('Customer added',name.trim(),[{text:'Done',onPress:()=>router.replace('/(app)/customers')}]);};return <SafeAreaView className="flex-1 bg-[#F5F7FB]" edges={['top']}><ScrollView contentContainerClassName="gap-4 px-5 pb-28 pt-5" keyboardShouldPersistTaps="handled"><ScreenHeader title="Add customer" subtitle="Customer balances and purchase history update from real transactions."/><Input label="Full name" placeholder="Customer or business name" value={name} onChangeText={setName}/><Input label="Phone number" placeholder="+234..." value={phone} onChangeText={setPhone} keyboardType="phone-pad"/><Input label="Email address" placeholder="Optional" value={email} onChangeText={setEmail} keyboardType="email-address"/><Input label="Address" placeholder="Optional" value={address} onChangeText={setAddress}/><Input label="Opening balance" placeholder="0" value={balance} onChangeText={setBalance} keyboardType="numeric"/><Button title="Save customer" onPress={save} className="h-14 rounded-[5px]"/></ScrollView></SafeAreaView>}
