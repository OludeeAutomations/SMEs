import React,{useState}from'react'; import{Alert,ScrollView,Text}from'react-native'; import{SafeAreaView}from'react-native-safe-area-context'; import{useRouter}from'expo-router';
import Button from'@/components/Button'; import Input from'@/components/Input'; import{ChoiceChips,ScreenHeader}from'@/components/business-ui'; import{SurfaceCard}from'@/components/dashboard-ui'; import{useAuthStore}from'@/store/authStore'; import{useBusinessStore,useWorkspace}from'@/store/businessStore'; import{formatMoney,parseAmount}from'@/utils/format';
export default function RecordSaleScreen(){const router=useRouter(),workspace=useWorkspace(),addSale=useBusinessStore(s=>s.addSale),currency=useAuthStore(s=>s.business?.currency??'NGN');const[customerId,setCustomerId]=useState(''),[productId,setProductId]=useState(''),[item,setItem]=useState(''),[amount,setAmount]=useState(''),[quantity,setQuantity]=useState('1'),[payment,setPayment]=useState<'CASH'|'TRANSFER'|'CARD'>('CASH');const selectProduct=(name:string)=>{const product=workspace.products.find(p=>p.name===name);setProductId(product?.id??'');setItem(product?.name??'');setAmount(String(product?.sellingPrice??''));};const total=parseAmount(amount)*Math.max(1,parseAmount(quantity));const save=()=>{const qty=Math.max(1,parseAmount(quantity)),product=workspace.products.find(p=>p.id===productId),customer=workspace.customers.find(c=>c.id===customerId);if(!item.trim()||parseAmount(amount)<=0)return Alert.alert('Check sale','Enter an item and a valid unit price.');if(product&&product.stockQuantity<qty)return Alert.alert('Not enough stock',`Only ${product.stockQuantity} units are available.`);const sale=addSale({customerId:customer?.id,customerName:customer?.fullName,items:[{productId:product?.id??'custom',productName:item.trim(),quantity:qty,price:parseAmount(amount)}],subtotal:total,total,paymentMethod:payment});router.replace(`/(app)/sales/${sale.id}` as never);};return <SafeAreaView className="flex-1 bg-[#F5F7FB]" edges={['top']}><ScrollView contentContainerClassName="gap-4 px-5 pb-28 pt-5" keyboardShouldPersistTaps="handled"><ScreenHeader title="Record new sale" subtitle="Choose an inventory product or enter a custom item/service."/>{workspace.customers.length?<><Text className="text-xs font-bold text-[#475569]">CUSTOMER (OPTIONAL)</Text><ChoiceChips options={['Walk-in',...workspace.customers.map(c=>c.fullName)]} value={workspace.customers.find(c=>c.id===customerId)?.fullName??'Walk-in'} onChange={name=>setCustomerId(workspace.customers.find(c=>c.fullName===name)?.id??'')}/></>:null}{workspace.products.length?<><Text className="text-xs font-bold text-[#475569]">INVENTORY (OPTIONAL)</Text><ChoiceChips options={workspace.products.map(p=>p.name)} value={workspace.products.find(p=>p.id===productId)?.name??''} onChange={selectProduct}/></>:null}<Input label="Item or service" placeholder="What did you sell?" value={item} onChangeText={value=>{setItem(value);setProductId('')}}/><Input label="Unit price" placeholder="0" value={amount} onChangeText={setAmount} keyboardType="numeric"/><Input label="Quantity" placeholder="1" value={quantity} onChangeText={setQuantity} keyboardType="numeric"/><Text className="text-xs font-bold text-[#475569]">PAYMENT METHOD</Text><ChoiceChips options={['CASH','TRANSFER','CARD']} value={payment} onChange={value=>setPayment(value as typeof payment)}/><SurfaceCard><Text className="text-[11px] text-[#475569]">TOTAL</Text><Text className="mt-2 font-mono text-xl font-bold text-[#2563EB]">{formatMoney(total,currency)}</Text></SurfaceCard><Button title="Complete sale" onPress={save} className="h-14 rounded-[5px]"/></ScrollView></SafeAreaView>}
